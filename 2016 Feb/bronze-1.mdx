http://www.usaco.org/index.php?page=viewproblem2&cpid=615

```cpp
#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main() {
    freopen("square.in", "r", stdin);
    freopen("square.out", "w", stdout);
    
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    ll x1, y1, x2, y2, X1, Y1, X2, Y2;
    cin >> x1 >> y1 >> x2 >> y2 >> X1 >> Y1 >> X2 >> Y2;
    
    ll l1 = max(x2, X2)-min(x1, X1);
    ll l2 = max(y2,Y2)-min(y1, Y1);
    cout << pow(max(l1, l2), 2);
    return 0;
}
```
